---
interface Props {
  label?: string;
  position?: "left" | "right";
}

const { label, position = "right" } = Astro.props;
---

<aside class="note" data-position={position}>
  {label && <span class="note-label">{label}</span>}
  <slot />
</aside>
